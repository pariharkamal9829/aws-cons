# Copy this file to `.env` and set real values if you want to use a remote MongoDB.
# If you don't set MONGO_URL and DB_NAME, the backend will use a local JSON file store (backend/data/).

MONGO_URL=
DB_NAME=
CORS_ORIGINS=*
